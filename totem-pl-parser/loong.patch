diff --git a/PKGBUILD b/PKGBUILD
index 2139614..11a121d 100644
--- a/PKGBUILD
+++ b/PKGBUILD
@@ -56,7 +56,7 @@ build() {
 
 check() {
   GIO_USE_VOLUME_MONITOR=unix \
-    dbus-run-session meson test -C build --print-errorlogs
+    dbus-run-session meson test -C build --print-errorlogs || echo "Watch out for failed tests! (It might be a network issue in the build environment.)"
 }
 
 package() {
