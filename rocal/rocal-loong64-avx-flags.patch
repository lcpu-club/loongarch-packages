diff --git a/rocAL/CMakeLists.txt b/rocAL/CMakeLists.txt
index 6112f69..0826f39 100644
--- a/rocAL/CMakeLists.txt
+++ b/rocAL/CMakeLists.txt
@@ -345,7 +345,13 @@ if(${BUILD_ROCAL})
     # -mfma -- Support MMX, SSE, SSE2, SSE3, SSSE3, SSE4.1, SSE4.2, AVX and FMA built-in functions and code generation
     # -mf16c -- Support F16C built-in functions and code generation
     # -Wno-deprecated-declarations -- Do not warn about uses of functions, variables, and types marked as deprecated by using the deprecated attribute
-    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -mavx2 -mfma -mf16c -Wno-deprecated-declarations")
+    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -Wno-deprecated-declarations")
+    # -mavx2/-mfma/-mf16c are x86-only flags. The AVX2 code paths are
+    # guarded by __AVX2__ (see master_graph.h/cpp) and fall back to
+    # scalar code on other architectures (e.g. loongarch64).
+    if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(x86_64|AMD64|amd64|i.86)$")
+        set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -mavx2 -mfma -mf16c")
+    endif()
     message("-- ${White}rocAL -- CMAKE_CXX_FLAGS:${CMAKE_CXX_FLAGS}${ColourReset}")
     target_link_libraries(${PROJECT_NAME} ${LINK_LIBRARY_LIST})
     message("-- ${White}rocAL -- Link Libraries: ${LINK_LIBRARY_LIST}${ColourReset}")
diff --git a/rocAL/include/pipeline/master_graph.h b/rocAL/include/pipeline/master_graph.h
index 2fd8f62..f903def 100644
--- a/rocAL/include/pipeline/master_graph.h
+++ b/rocAL/include/pipeline/master_graph.h
@@ -65,7 +65,7 @@ THE SOFTWARE.
 #if ENABLE_SIMD
 #if _WIN32
 #include <intrin.h>
-#else
+#elif defined(__x86_64__) || defined(__i386__)
 #include <immintrin.h>
 #include <smmintrin.h>
 #include <x86intrin.h>
