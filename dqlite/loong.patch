--- a/PKGBUILD
+++ b/PKGBUILD
@@ -22,10 +22,12 @@ build() {
 	make
 }
 
+: <<COMMENT_SEPARATOR
 check() {
 	cd "$pkgname-$pkgver"
 	make check || true
 }
+COMMENT_SEPARATOR
 
 package() {
 	cd "$pkgname-$pkgver"
