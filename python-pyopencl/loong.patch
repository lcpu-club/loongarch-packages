diff --git a/PKGBUILD b/PKGBUILD
index 252bd0c..cd18ded 100644
--- a/PKGBUILD
+++ b/PKGBUILD
@@ -33,7 +33,6 @@ makedepends=(
 )
 # NOTE: we install all available OpenCL drivers so that tests don't fail on PLATFORM_NOT_FOUND_KHR
 checkdepends=(
-    'intel-compute-runtime'
     'opencl-clover-mesa'
     'opencl-nvidia'
     'opencl-rusticl-mesa'
