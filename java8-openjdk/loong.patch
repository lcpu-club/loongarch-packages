--- a/PKGBUILD
+++ b/PKGBUILD
@@ -49,6 +49,7 @@ b2sums=('4b9450c0c5a442f2447218ef6af2f409120115e7e4ac4bcdc495226627dc89d8f86e71a
 case "${CARCH}" in
   'x86_64'  ) _JARCH=amd64   ; _DOC_ARCH=x86_64  ;;
   'aarch64' ) _JARCH=aarch64 ; _DOC_ARCH=aarch64 ;;
+  'loong64') _JARCH=loongarch64; _DOC_ARCH=loongarch64 ;;
 esac
 
 _jdkname=openjdk8
@@ -63,7 +64,7 @@ _nonheadless=(
 )
 
 prepare() {
-  cd jdk8u-jdk${_majorver}u${_minorver}-b${_updatever}
+  cd jdk8u-jdk${_majorver}u${_minorver}-b${_updatever}-ls-ga
 
   # Do not treats warnings as errors
   sed -E -i 's/(^WARNINGS_ARE_ERRORS = -W)(error)/\1no-\2/' \
@@ -83,7 +84,7 @@ END
 }
 
 build() {
-  cd jdk8u-jdk${_majorver}u${_minorver}-b${_updatever}
+  cd jdk8u-jdk${_majorver}u${_minorver}-b${_updatever}-ls-ga
 
   unset JAVA_HOME
 
@@ -136,7 +137,7 @@ build() {
 }
 
 check() {
-  cd jdk8u-jdk${_majorver}u${_minorver}-b${_updatever}
+  cd jdk8u-jdk${_majorver}u${_minorver}-b${_updatever}-ls-ga
   #make -k test
 }
 
@@ -337,8 +338,11 @@ package_openjdk8-doc() {
   pkgdesc='OpenJDK Java 8 documentation'
 
   install -d -m 755 "${pkgdir}/usr/share/doc/${pkgbase}/"
-  cp -r "${srcdir}"/jdk8u-jdk${_majorver}u${_minorver}-b${_updatever}/build/linux-${_DOC_ARCH}-normal-server-release/docs/* \
+  cp -r "${srcdir}"/jdk8u-jdk${_majorver}u${_minorver}-b${_updatever}-ls-ga/build/linux-${_DOC_ARCH}-normal-server-release/docs/* \
     "${pkgdir}/usr/share/doc/${pkgbase}/"
 }
 
 # vim: ts=2 sw=2 et:
+
+source[0]="https://github.com/loongson/jdk${_majorver}u/archive/refs/tags/jdk${_majorver}u${_minorver}-b${_updatever}-ls-ga.tar.gz"
+b2sums[0]='01d94b935970b859c95949f6d36bafde5c92b2200bed94c4653a9c02e8496a61549f200fab538c7b13e2e6c381900acd5c95b946e89abd5774a8c091763d47d0'
