--- a/PKGBUILD
+++ b/PKGBUILD
@@ -24,7 +24,7 @@ build() {
 
 check() {
   cd build
-  LD_LIBRARY_PATH="$(pwd)"/lib/ ctest -v --output-on-failure
+  LD_LIBRARY_PATH="$(pwd)"/lib/ ctest --output-on-failure
 }
 
 package() {
