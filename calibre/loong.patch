diff --git a/PKGBUILD b/PKGBUILD
index f14f4ac..02ad6da 100644
--- a/PKGBUILD
+++ b/PKGBUILD
@@ -139,7 +139,9 @@ build() {
 check() {
 	cd "$_archive"
 	export LANG='en_US.UTF-8'
-	python setup.py test --under-sanitize --exclude-test-name test_piper
+	# test_feedparser imports the removed sgmllib module; fixed upstream by using
+	# feedparser_sgmllib instead. Drop the exclusion once the fix is in a release.
+	python setup.py test --under-sanitize --exclude-test-name test_piper --exclude-test-name test_feedparser
 	python setup.py test_rs
 }
 
