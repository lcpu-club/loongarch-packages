--- a/PKGBUILD
+++ b/PKGBUILD
@@ -61,3 +61,8 @@ package() {
 
   DESTDIR="$pkgdir" cmake --install build
 }
+
+prepare() {
+  cd $pkgname-$pkgver
+  sed -i 's/-mno-omit-leaf-frame-pointer//g' tests/testprogs/CMakeLists.txt
+}
